// Global Jest teardown - runs once after all test suites
export default async function globalTeardown() {
  // Nothing to do here currently, but keeping for future use
  // The database connections are closed in afterEach hooks
}
