/**
 * Finds the best origin for the market maker transfer from the list of invoices
 * that are being selected from.
 */
export const selectOrigin = async () => {
  throw new Error(`not implemented - selectOrigin`);
};
