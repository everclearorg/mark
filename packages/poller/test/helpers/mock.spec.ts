import { expect } from 'chai';
import sinon, { SinonStub } from 'sinon';

describe('#TEST', () => {
  describe('#getUnauthenticatedTokenPrice', () => {
    it('should return 2 when adding 1 + 1', () => {
      expect(1 + 1).to.eq(2);
    });
  });
});
